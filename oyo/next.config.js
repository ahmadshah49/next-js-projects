/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode:true,
    images:{
        domains:["static.leonardo-hotels.com", "images.oyoroomscdn.com",]
    }
}

module.exports = nextConfig
